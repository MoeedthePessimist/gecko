"use client";
import { cn } from "@/lib/utils";
import React from "react";

const Header = () => {
  return <div className={cn("w-full bg-primary h-12 fixed z-10")}>Header</div>;
};

export default Header;
